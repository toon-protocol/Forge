{
  "name": "@toon-protocol/forge-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Forge — the factory manager for the toon-protocol software-factory substrate.",
  "license": "MIT",
  "packageManager": "pnpm@8.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
