# Coding Standards

Loaded by the reviewer agent during code review (`@.sandcastle/CODING_STANDARDS.md`), so these
are enforced at review time without costing implementation tokens.

Fill in this factory's real coding standards here: style conventions, testing expectations, and
architectural rules specific to this repo. Until this file is filled in, the reviewer falls back
to general engineering judgment (clarity, test coverage, no unchecked `any`, no injected
vulnerabilities — see `review-prompt.md`'s built-in checklist).
