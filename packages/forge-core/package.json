{
  "name": "@toon-protocol/forge-core",
  "version": "0.0.0",
  "description": "Manifest loader + inner-gate injection + per-role model tiering wrapping @ai-hero/sandcastle. Full label→plan→implement→review→PR orchestration not yet implemented — see Forge#8.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@ai-hero/sandcastle": "0.12.0",
    "smol-toml": "^1.7.0"
  }
}
