{
  "name": "@toon-protocol/forge-core",
  "version": "0.0.0",
  "description": "Manifest loader + inner-gate injection + per-role model tiering wrapping @ai-hero/sandcastle, composing the full label→plan→implement→inner-gates→review→PR cycle (runCycle) and its concrete sandcastle-backed phase runners (createSandcastleRunners). Driving it from a real Actions run is Forge#24.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@ai-hero/sandcastle": "0.12.0",
    "smol-toml": "^1.7.0"
  }
}
